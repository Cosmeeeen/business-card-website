import React from 'react';

const SocialButtons = () => {
    return (
        <div className="socialButtonsContainer">
            <h1>These are the social buttons.</h1>
        </div>
    );
}

export default SocialButtons;